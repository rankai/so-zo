<% @publishes.each do |publish| %>
      <div class="col-sm-4">
          <div class="graybox">
            <div class="overflow">
              <a href="#">
                <%= image_tag publish.publish_image.url(:medium), :width => "342px" %>
              </a>
            </div>
            <p>
              <%= publish.name %>
            </p>
          </div>
          <!-- graybox -->
        </div>
        <!-- col-sm-4 -->
  <% end %>