<!-- 
	2014-07-10
	version 0.1
	comments: 未国际化
 -->
<nav class="navbar navbar-tabs" role="navigation">
   <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" 
         data-target="#example-navbar-collapse">
         <span class="sr-only">切换导航</span>
         <span class="icon-bar"></span>
         <span class="icon-bar"></span>
         <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: "navbar-brand" do %>
      	<%= image_tag "new-logo.png", alt: "itabeya" %>
      <% end %>
   </div>
   <div class="collapse navbar-collapse" id="example-navbar-collapse">
      <ul class="nav navbar-nav">
      	 <li class="<%= current_page?('/') ? "active" : ""  %>">
      	 	<%= link_to "トップページ", root_path %>
      	 </li>
         <li class="<%= current_page?(controller: '/publishes', action: 'index') ? "active" : ""  %>">
         	<%= link_to "投稿された作品を見る", publishes_path %>
         </li>
         <li class="<%= current_page?(controller: '/products', action: 'index') ? "active" : ""  %>">
         	<%= link_to "痛インテリアを探す", products_path %>
         </li>
          <li>
          	<% if user_signed_in? %>
         		<%= link_to "痛インテリアを作る", new_user_publish_path(current_user) %>
         	<% else %>
         		<%= link_to "痛インテリアを作る", new_user_session_path %>
         	<% end %>
         </li>
         <!-- 尝试失败：屏幕缩小到768以下后，自动添加子类型到顶级导航条目下，但是这样的话顶级条目就无法点击跳转了 -->
         <!-- <li>
		    <a href="<%= publishes_path %>" class="dropdown-toggle" data-toggle="dropdown">
		       作品投稿 <b class="caret"></b>
		    </a>
		    <ul class="dropdown-menu">
		       < publish_categories.each do |category| %>
                    <li><= link_to category.description, publishes_path(publish_category: category.name) %></li>
                < end %>
		    </ul>
		 </li> -->
         <li class="loginbar" id="login">
         	<% if user_signed_in? %>
         		<%= render "home/user_dropdown" %>
         	<% else %>
         		<%= link_to t('nav.login'), new_user_session_path %>
         	<% end %>
         </li>
      </ul>
   </div>
</nav>