<%= form_for(@basis) do |f| %>
  <% if @basis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@basis.errors.count, "error") %> prohibited this basis from being saved:</h2>

      <ul>
      <% @basis.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :detail %><br>
    <%= f.text_area :detail %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
