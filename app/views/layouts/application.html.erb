<!DOCTYPE html>
<html>
<head>
  <meta name="description" content="SO-ZOは「人の想像を創造する」を理念とし、オーダーメイドのインテリア商品に特化した、「痛部屋」を創るサービスです" />
  <meta name="keywords" content="so-zo,痛部屋,痛商品,いたべや,想像を創造,アニメ壁紙,オーダーメイド" />
  <title><%= t('website.title') %></title>
  <%= javascript_include_tag "application" %>
  
  <%= stylesheet_link_tag    "reset" %>
  <%= stylesheet_link_tag    "style" %>
  <%= stylesheet_link_tag    "lightbox" %>
  <%= stylesheet_link_tag    "bootstrap.min" %>  
  <%= stylesheet_link_tag    "buttons" %>
  <%= stylesheet_link_tag    "icheck/skins/flat/green" %>
  <%= stylesheet_link_tag    "home" %>
  <%= stylesheet_link_tag    "publishes" %>
  <%= stylesheet_link_tag    "products" %>
  <%= stylesheet_link_tag    "application" %>

  <%= csrf_meta_tags %>

  <!-- Bootstrap -->
  <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media
  queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file://
  -->
  <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js">
      </script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js">
      </script>
  <![endif]-->

  <!--<style type="text/css">
    .alert {
      display: none
    }
  </style>-->


  <script type="text/javascript">

    $(document).ready(function() {

        if (window.location.href.indexOf('#_=_') > 0) {
            window.location = window.location.href.replace(/#.*/, '');
        }

       /* $(".alert").slideDown('slow');*/
        $(".alert").each(function(){
          if($(this).is(":visible")) {
             window.setTimeout(function() {
                $(this).fadeTo(500, 0).slideUp(500, function(){
                    $(this).hide(); 
                });
              }, 2000);
          }
        });
       
        $("img.lazy").lazyload({
            event : "sporty",
            effect: 'fadeIn'
        });

        $("input[type='checkbox']").iCheck({
          checkboxClass: 'icheckbox_flat-green',
        });

        // use bootstrap nav instead of headroom
        // grab an element
        // var myElement = document.querySelector(".header #nav");
        // // construct an instance of Headroom, passing the element
        // var headroom  = new Headroom(myElement);
        // // initialise
        // headroom.init();

    });


    $(window).bind("load", function() { 
        var timeout = setTimeout(function() { $("img.lazy").trigger("sporty") }, 1500);
    });         

  </script>

</head>

<body>
  
  <!-- flash.each do |name, msg| -->
          <!-- content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) -->
<!-- end -->

 <%= render "/layouts/alerts" %>

  <% if notice %>
    <div class="alert alert-info alert-dismissable" style="text-align:right">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <span><%= notice %></span>
    </div>
  <% end %>

  <% if alert %>
    <div class="alert alert-warning alert-dismissable" style="text-align:right">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <span><%= alert %></span>
    </div>
  <% end %>

	<%= yield %>

</body>
</html>
